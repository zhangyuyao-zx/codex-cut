# 独立迁移记录

来源为旧项目的本地定版提交 `41c626ef6b9eb2f7fb310e83556d9aaf17e7bdf0`（标签 `codex-cut-workbench-v0.1.0`）。本目录建立独立 Git 仓库，不复制旧仓库完整 App 历史。

## 提取范围

保留完整工作台入口、场景 manifest 与 saved 动画源文件；按 TypeScript AST 解析普通 import、export、动态字面量 import、require 与 import 类型表达式，复制所需本地传递依赖。具体旧新路径映射见 MIGRATION.json。

旧 App 壳、内置 Agent 执行器、旧原生播放器、研究组件目录、实验网页、编译产物和旧 node_modules 不迁入。部分兼容数据类型沿用旧名称，不代表启动旧 Agent。

原项目代码和工程没有移动、修改或删除。当前工程采用独立实体副本；APFS 克隆可共享初始存储块，但修改时独立，不是软链接。项目媒体与参数共3196文件、1778450813字节，复制后 SHA-256 全部匹配；排除 qa、verification 和进程 runtime。

浏览器、合成器复制到本目录 runtime/remotion；安装本目录自己的锁定 npm 依赖。继续依赖本机 Node、FFmpeg/ffprobe、Whisper 与模型，不宣称全新电脑免安装。

## 路径与备份

运行源码改用 modules 下的路径；渲染源身份读取、运行时定位与工程快照代码范围一并更新。旧 generated 文件中 sourcePath、审计哈希及历史文档路径保留为来源记录，不按这些路径执行旧代码。

当前工程的参数、确认、反馈及历史视频保持原始字节。源码路径和依赖锁变化可能使已有渲染指纹过期；此时依照原规则重新生成受影响结果，不能伪造当前确认状态。

页面备份捕获新的 workbench-local、modules、public、package/lock、根 README 和源码映射，配对当前工程。runtime/remotion 另行备份，不由 Git 或页面快照保存。旧 App 快照继续在旧项目中恢复，不混入新的备份列表。

## 检查范围

本次只检查迁移完整性、独立依赖、启动和既有工程读取，以及迁移涉及的快照范围；不恢复全库测试、批量渲染或用户成片验收。最终检查结果追加于下方。

## 迁移检查结果（2026-09-17）

- 193 个源码文件的本地模块引用检查：无缺失引用，无解析到独立目录之外的依赖。
- 独立目录类型检查通过；调整后的备份/恢复4项检查通过，仅使用临时夹具。
- 启动后组件库194项、制作方案修订27、12个视觉段落；场景代码无错误。
- 浏览器入口与实际 JavaScript 模块返回正常；关键模块路径均在新目录。
- 工程3196文件 SHA-256全部一致；启动后粗剪与制作方案核心JSON仍匹配原哈希。
- 源码、公共资源、运行时与工程没有指向旧目录的软链接。
- 正式4340服务根路径为独立目录；原目录服务已安全停止，旧仓库工作树仍干净。
- 插件源码与安装副本的本机配置均改为新根目录；新启动的插件桥接可识别新服务。当前Codex窗口内已经加载的旧MCP连接仍缓存原路径，需要重新加载该连接（或重启Codex）后使用；没有伪装旧服务身份绕过检查。
- 未运行全库测试、没有生成新的视频，没有改动用户的确认记录。

## 回退

先保存页面内容，在新目录执行 `npm run stop`。原项目仍完整保留，可从原目录启动原工作台；不要让两个目录争用4340。插件原路径配置备份在工作台根 `90-归档/Codex-Cut开发结项-20260917/插件迁移配置备份/`，需恢复配置并重新加载插件连接后才与旧服务配对。回退不自动把新工程的后续调整覆盖回旧工程。
