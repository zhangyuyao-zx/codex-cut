# 工作台交互打磨（2026-09-17）

## 已确认范围

保持 Codex 对话、轻量工作台、六步视觉设计、逐段渲染确认后合成。基于当前工程改造操作体验，保留既有素材、视频、动画实现和参数保护机制。

## 本轮代码

- 包装页分开编辑画面与审阅样片，历史与粗剪放入版本菜单。段落主操作按未保存、生成中、待审阅、可确认、已确认呈现；已确认后提供下一段。
- 整片合成、下载、整片确认集中到左侧整片导出，不与当前段落操作混放。
- 反馈、口播与动作、设计说明分栏；有意义的动作和完整口播用于定位，旧占位标签不再显示为单字分镜。
- 属性增加单位、步进、默认值重置；用户锁与参数覆盖仍使用原机制。
- 组件库按用途过滤，12项分页，按需加载真实实现的静态缩略图；缩略图使用明确示例素材，不把示例写入影片。
- 组件添加目标默认跟随当前段落；保存后可直接返回该段编辑画面。
- 组件可保存为本地参数预设，在“我的预设”再次选用。预设独立文件原子保存，排除原段落时间和素材引用。任意原创动画的源码模板化不等于参数预设，仍需独立适配。
- 剪辑页自然段文稿、点击定位、原生拖选、Shift 连选、删除前预览、恢复，以及按时长显示的片段条已接入。恢复范围向外对齐30fps帧边界，避免删除后恢复遗留空隙。
- 窄窗口采用上下排列且侧栏独立滚动；正常桌面保持三栏，侧栏可收起。
- 实际叠加组件不再携带组件预览容器的整块深色背景；组件本身有意设计的卡片底色保留。

## 验证记录

- [x] 工作台与根项目 TypeScript 通过；相关8文件40项测试通过，剪辑交互与服务12项测试通过。透明预览参数整合后再次通过工作台类型检查。
- [x] 包装页参数1→1.2→重置1、放弃修改、未保存保护、编辑/样片切换与口播定位实际操作通过。未代替用户点击段落确认。
- [x] 组件静态缩略图真实显示；隔离副本添加文字组件后在段落中可见，保存参数预设、刷新后读取文字/字号/位置通过。不是194项逐一视觉验收。
- [x] 原生拖选3词、删除、Shift连选3词、恢复实测通过；恢复后从17段80.13秒回到16段81.10秒。
- [x] 900px及720px宽度无横向溢出；720px的面板覆盖已修复，面板边界不重叠；收起侧栏保持16:9预览。
- [x] 当前正式制作与粗剪数据哈希保持，测试修改位于独立副本。已认可视频未重新渲染或覆盖。

隔离副本：`/private/tmp/codex-cut-interaction-qa-20260917`。只做元数据操作，不在指向原始渲染目录的链接里生成文件。正式项目 `production/production.json` SHA-256 为 `d29241ceebac2b62bba601acaf7576d6b6e5d56ce0d6868d408e5768e238e72f`；`cut/roughcut.json` 为 `c96e95774496f4e18cf1f53e03ae007f63f5d421f094f33ec859487560bb03b0`。

## 尚不能宣称的结果

这次操作界面改造不等于194项组件逐项视觉认可，不自动重写原片动画，不替用户确认任何段落。组件参数预设不承诺任意场景源码一键迁移到任意口播。整个六项升级按实际交付和验收记录推进，不将规范文字当作已完成能力。
