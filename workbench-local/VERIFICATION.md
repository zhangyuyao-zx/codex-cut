# P0/P1 验证记录 · 2026-09-17

## 已完成

- 独立入口 `workbench-local/`，旧 App、旧组件及原始素材均保留。
- 实际浏览器打开 http://127.0.0.1:4340，检查 16:9 预览和三列属性布局。
- UI 将标题改成“每一句，”，锁定并保存，刷新后文案和锁仍在。
- CLI 调整 motion.offsetFrames=2，标题保持不变；尝试改锁定标题被拒绝。
- UI 两次撤销恢复原动画参数与原文案。
- 存储测试 5 项通过：持久化、并发修订、锁、撤销重做、非法/损坏数据等。
- TypeScript 检查通过。
- 实际导出 1920×1080 / 30fps / 341帧 / 11.366667秒 MP4。
- 检查起始、转写、对齐三阶段的实际导出帧，画面与认可样片一致。
- 修复初次 Remotion 音频导出的偏移，改为对画面单独渲染后无损封装原始口播。最终导出与源音频比较，8kHz单声道解码样本零时移相关系数1.0；音轨11.366秒。
- 同源最终视频与认可基线全341帧SSIM=0.999998。

## 范围与边界

- 当前为单段样片工作台，不是完整视频编辑器。
- 属性更新即时预览，点击保存后持久化；刷新前未保存内容有离开提醒。
- 项目历史仅覆盖参数，不覆盖动画源代码；代码版本仍由Git管理。
- 参数锁约束Codex协议写入，不是对本机任意磁盘操作的安全隔离。
- 存储串行化限单进程，服务固定监听127.0.0.1:4340；CLI只走服务API。
- 已验证导出音轨与原音源一致；未声称完成真人主观听感验收或所有设备播放性能验收。
- 初始媒体路径仍为本机样片路径，后续导入阶段将替换为可移植项目素材绑定。
- 新工作台的审美与交互仍待用户确认。既有组件全部保留，本轮尚未接入组件浏览器。

可再生成的验证帧和数据在 `projects/local-workbench/qa/`。导出在 `projects/local-workbench/exports/`，项目修订在 `projects/local-workbench/project.json`。

# P2 单素材粗剪 · 2026-09-17

- 新增 Roughcut UI、服务、API及Codex CLI操作；未接入第二个Agent。
- 实际完成路径导入与浏览器上传接口，原素材保留；显示原文件名，新素材导入前自动备份上一份剪辑工程。
- UI调用本地Whisper small真实转写，44词；既有转写JSON导入也通过。模型未产生外部媒体上传。
- UI删除3–4秒：341帧变311帧；视频10.366667秒，音轨10.367秒。实际查看89/90/91帧，切点无黑场。
- 原词4.16秒映射到剪后3.16秒；实际UI点击文稿跳转显示3.16秒。
- UI勾选“所以”删除对应时间，再撤销恢复完整口播。
- UI把识别的“主字稿”中的“主”修正为“逐”，源时间仍为6.84秒。
- 最终EDL对齐30fps帧网格，音视频与文稿映射使用同一组实际区间。7段非整帧输入归一化后预期232帧，实测232帧/7.733333秒，音轨7.733秒；与按同一区间裁切的源代理音频相比，零时移相关系数0.99864。
- 服务与项目存储共14项测试通过；复用的转写服务14项测试通过；TypeScript检查通过。
- 持久化busy在服务重启时明确转为中断错误，可重试。服务先取得独占本机端口后才执行恢复，避免另一个启动实例改写运行中的任务状态。
- 当前展示素材为原口播画面与已验证音源组成的11.37秒测试片段；测试删段已撤销。原动画样片保留。

边界：单视频带音轨、正常速度、1280宽工作代理；暂无多素材、变速、跨段转场、原始4K母版输出和自动连接包装场景。词级ASR仍需人工校对；按词剪切边界最多向保留区间内部取整一帧，避免长期时间累计漂移。素材及代理保留未做自动清理。新工作台的真人听感、长片性能和用户审美仍需后续实际验收。


# P3 制作与审阅首条链路 · 2026-09-17

- 当前粗剪revision13，三段设计引用稳定词ID；计划revision7。演示采用当前真实口播及已编写的动画程序，未调用外部模型。
- 25项测试通过：样片存储5、粗剪9、制作存储7、词级设计绑定4。覆盖并发修订冲突、撤销、已删除词失效、换素材失效和剪后时刻迁移。TypeScript通过。
- 实际渲染最终MP4：1920×1080、341帧、视频11.366667秒、音轨11.367秒。与本次输入粗剪的534个音频包SHA256逐一完全一致。
- 检查了最终三个阶段的真实渲染帧。画面为人物/标题、文稿出现、同一组词落到时间线；未把设计说明作为一行字幕渲染。
- 实际浏览器检查三栏布局、16:9画面、段落定位、标题草稿的切页保护和放弃恢复。
- 实际记录时间反馈，读回段落、时间及方案修订；反馈不使已有视频失效。验收反馈随后按明确revision撤销，没有替用户认可画面。
- 实际新增待补需求、上传测试图片、完成包含该图片的渲染并查看真实帧；图片出现在配置的transcript槽位。撤销上传后UI显示待补充、旧审阅过期且认可按钮禁用。测试需求及图片绑定已撤销，原文件与渲染历史保留。
- 最终干净视频：`projects/local-workbench/production/renders/4a683c9a-14cd-4ae3-8d25-1700d0a21a91/review.mp4`。QA证据：`projects/local-workbench/qa/p3/media-check.json`与`frames.jpg`。
- 未实测：真人听感、长片和4K播放性能、多项目迁移、多个素材槽位、任意新口播的完整原创动画、缺素材草稿的最终水印渲染和认可接口整条路径。后两项已实现但本轮只检查了状态/代码；没有代替用户做审美认可。

这轮完成首个设计到实际成片的贯通样例，不能据此声称P3所有内容类型或P4组件迁移已完成。新增题材仍由Codex编写场景实现；布局、复杂动效和素材选择不是固定模板自动推断。

# P4 历史实验记录（797项目录已被后续194项 App 库替代）· 2026-09-17

- 只读核对740个候选、486个upstream manifest条目（453个安装入口）及209个Shotcraft独立样式。目录按旧ID对应关系合并，最终797项、无重复ID；保留57个额外样式。API报告502项可加载源码，154项当前可读取参考视频。
- 新增参数提取/身份映射/输入边界测试3项；总计28项测试通过。验证不会把内部硬编码常量当成props，也不会执行源码来提取默认值；未知参数拒绝、路径穿越ID拒绝。
- 浏览器实际搜索rs-text-typewriter，动态加载TextTypewriter。修改文字为“口播变文字”，位置1000/650，缩放0.5，出现词word-15、结束词word-23，写入当前段落并读回。
- 实际导出该组件：从第115帧开始，共51帧。检查114、144、166帧，确认出现前没有、出现时内容与位置正确、结束后消失。导出音轨534包与粗剪输入逐包SHA256一致。
- 最终包含资源复制的验证渲染：`projects/local-workbench/production/renders/5b6ea91f-b199-46d5-a6e2-1dd09fee555d/review.mp4`；公共资源已进入该次media目录。验证图片及报告在`projects/local-workbench/qa/p4/`。
- 测试组件通过明确revision撤销，干净三段设计保留。没有替用户认可测试画面；组件原生黑背景在验证帧中保留，不把它作为最终审美方案。
- 边界：没有声称502个入口均已在新工作台逐一运行。参考视频只是参考，Live Player失败会报错；不支持的输入形状、硬编码文案、外部资源和复杂场景仍需Codex适配。源码递归依赖尚未完整冻结，渲染期间不得修改源码。

- 实际打开ai-chat-reveal参考视频，媒体readyState=4、时长19.333秒；不显示直接加入按钮。最终干净三段审阅revision11：`projects/local-workbench/production/renders/cbfbf4bc-66ab-49c7-8392-59be95707323/review.mp4`。

# P4 范围纠正：App 最终盘点库 · 2026-09-17

- 当前唯一目录为 App COMPONENT_LIBRARY：194项，19个基础组件、175个运行时组件；不再迁移797项研究候选，不保留单独研究入口。
- 全部194项字段与原 App 定义逐项对照；复用原参数校验器，包括嵌套图表数据。28项测试及 TypeScript 检查通过。
- 使用实际 App 渲染器完成柱状图第60帧渲染，人工检查标题、两项标签、数值3/5及柱高正确。可再生成证据：projects/local-workbench/qa/app-library/{entry.tsx,check.mjs,chart.png}。
- 修正 Remotion 导出对旧 App .js→TypeScript 导入的解析；单素材组件从对应词级起点截取视频。
- 本轮没有改动用户场景，也没有作出审美认可。多素材与相邻镜头绑定、194项逐项播放和完整导出仍待验收。浏览器控制会话异常，本轮未完成属性面板的交互验收；目录API和真实渲染已验证。

# P4 多素材选用与显式转场 · 2026-09-17

- 新增组件素材服务，逐槽绑定图片、视频或粗剪；视频起点独立保存。参数与媒体缺失、区间不足在提交/渲染前校验。绑定随方案修订撤销。
- 页面实测：选择“前后对比揭示”，填写标题与两侧标签，分别选择人物图与图表；两个 Img 加载完成，页面无异常。未保存参数阻止切页，素材/属性面板独立滚动，预览仍为16:9。
- 真实上传 PNG 和11.37秒 MP4（341帧）；素材服务子任务另验证了 VP9 WebM→H.264 MP4。原始素材未覆盖。
- 真实整段导出：revision12，`projects/local-workbench/production/renders/20b7e9cd-8fa7-4e72-8a27-9721749b699a/review.mp4`。双素材对比区间115–165帧，显式前后画面转场166–237帧。查看150/211帧，内容、标签、素材顺序正确；源口播534个音频包SHA256逐一完全一致。
- 修复原 App 对比组件写死的英文标题与被裁掉的右侧标签，标题进入原参数接口；更新组件实现指纹。旧 App 的组件集成与历史兼容13项检查通过。
- 精确按revision撤销测试组件，原三段方案恢复为revision13；没有替用户认可测试画面。上传测试素材和导出证据保留在项目中。
- 可再生成的脚本、图片、UI截图、输入与音轨报告：`projects/local-workbench/qa/component-inputs/`。测试素材仅验证输入区别，不是最终创作方案。
- 限制：目前转场是两份显式画面素材之间的效果，不能声称已经连接相邻的完整包装场景；194项未逐项验收。通用场景编写/接入、完整源码版本恢复仍属于后续工作。
- 最终检查：工作台36项测试、TypeScript通过；组件目录仍为194项。两份视频输入分别从0秒和6秒起，在组件第30帧得到不同的1秒/7秒画面，实际帧见VideoPoints-30.png。
- 集成过程中早期服务曾使用额外一层component-assets目录；最终服务直接使用传入目录。三份测试素材已按相同ID复制到最终目录并读回，旧测试副本保留，未丢失素材。
- 最终原方案重新导出完成且current=true：revision13，`projects/local-workbench/production/renders/bc85b646-8ad3-4755-8acf-ba70ee533594/review.mp4`。

# P3 通用场景接入 · 2026-09-17

- 新增 manifest 注册/参数校验、逐词时间绑定、统一 SceneModuleSurface 和单场景 Player。接入 hero-keyword 与 word-flow 两种独立场景，测试使用 opening-new / flow-new 新段落 ID。
- TypeScript 与45项测试通过。覆盖路径/软链接越界、无执行读取、参数类型/范围、缺失入口、粗剪改时和不存在的词、不能静默丢弃补充素材。
- 真实整段导出：`projects/local-workbench/production/renders/e2299b0a-8227-4f9d-8072-24a5fa8ad5f6/review.mp4`。查看人物观点换行和三步卡片逐项出现的实际帧；卡片局部出现帧为0、51、123，来自口播词15、24、33。
- 前一相同音轨导出 f8c46e8f-7f8e-4c6a-bf58-583c8683b5dd 的534个音频包与连续粗剪逐包SHA256一致。最终文字换行版也已完整解码通过，534个音频包逐包一致，记录在 `qa/scene-modules/final-proof.json`。
- 浏览器实测：字号68改82，预览立即变82px；未保存时导航锁定；点击保存设计后服务端revision16读回82，页面无异常。测试过程中自动化快捷键没有选中数字，改用输入框 fill 后通过，没有把测试输入失败当产品保存失败。
- 测试方案通过明确revision撤销，保留原三段方案；验证脚本、恢复记录、截图、导出记录在 `projects/local-workbench/qa/scene-modules/`，属于可再生成的验证产物。没有替用户确认新画面的审美。
- 导出保留编译bundle与入口源码副本，但不是递归依赖完整历史；新场景源代码修改期间不得并行导出。通用补充素材路径已接通，本轮未逐种素材完成实际画面验收。

- 最终恢复原三段方案到revision19并重新导出，current=true：`projects/local-workbench/production/renders/0965f3eb-4ba3-4b66-bc29-e546f24caa7f/review.mp4`。

# P3 视觉方案与样片优先流程 · 2026-09-17

- 每段新增可选 design：核心表达、信息关系、主副视觉对象/素材来源/构图、按词触发的包装动作、设计理由。旧方案不伪造默认设计，显示待补；新设计校验对象引用及口播节拍引用，随修订保存、重开和撤销。
- 修复必须全片都有 program 才能保存的限制；允许先保存全片方案、只实现样片段落。全片渲染仍拒绝未实现的段落。
- 样片独立任务、视频与认可记录；按段落全局帧范围渲染，原声按同范围 atrim 后AAC编码。全片继续复制连续原声音轨。
- 隔离测试项目 `projects/local-workbench/qa/p3-workflow/project-1789617164626`：真实样片渲染71b825b4-f737-41bc-940c-5bda9a10a955，115至165帧，共51帧/1.7秒。完整解码及第30帧检查通过；81个音频包与独立相同区间裁切编码对照一致。整片渲染61ce2518-85ef-4aa6-a6a3-1af9abb3e683，共341帧。
- API实测：仅一段实现可保存/生成样片；错误认可key拒绝；其他段落改动不影响认可；补齐剩余实现后全片可生成；改已认可段落设计使认可失效并阻止继续全片。测试认可只在隔离副本中，未认可用户作品。
- 页面：入口改名、开发工具默认折叠、步骤说明可见；手动视觉方案草稿可以填写，未保存时切页保护有效，放弃后恢复。浏览器无pageerror。实际截图 `qa/p3-workflow/ui.png`。
- 52项测试、TypeScript与diff空白检查通过。用户项目仍为revision19，没有写入测试设计或认可。服务已重启，页面可连接。
- 此轮不是P3整体完成。通用对象参数锁、源码递归历史、精确局部影响/重渲染、渲染取消与失败后自动恢复上一可播放版本仍缺失；设计与代码视觉一致性仍需Codex检查实际画面及用户认可。详细逐阶段缺口见PLAN_STATUS.md。

# 对象编辑与修改保护 · 2026-09-17

- 移除后端与页面的 design 必填、样片认可解锁全片门槛；素材/参数/时间等实际有效性校验保留。
- 原三段动画与hero-keyword/word-flow声明的对象可点击选择，右侧显示对应控制项。原三段使用同一ProductionProgram渲染编辑画面和导出，人物参数/标题覆盖按当前段落生效。
- editor.overrides/locks与基础参数隔离保存。默认Codex批量方案修改保留用户覆盖；有用户调整的段落删除或换模块被拒绝；显式参数修改拒绝锁定字段。并发冲突、保存/重开/撤销测试通过。
- selection服务单独原子保存段落、对象、全片时间、视图、revision和草稿状态；不会增加创作修订。页面放弃草稿会即时更新该状态。
- 隔离项目 `qa/object-editing/project-1789618830203`：用户文字“用户保留的文字”、人物大小0.8与水平偏移30，在Codex改基础文字和动画入场时长后保持。先后整段渲染6f5969c0-5892-4ab4-b87b-963ccd2a78a6和af90ea40-2f59-428b-9fb6-355d39acc446；修改入场时长后的第15帧有变化；查看两份第60帧，文字、人物尺寸和位置保留。视频编码会引入像素差异，不将两帧声称为逐字节相同。两份视频完整解码，534个音频包均与原粗剪一致。
- 隔离源码故意写入语法错误，编译按预期失败；previousReview仍指向后一成功视频。原用户项目与正式场景源文件未注入测试错误。
- 浏览器实际点击原三段人物，将大小1改1.2，元素宽840→1008px，选中描边和属性面板可见；Codex context读到person和未保存草稿，放弃后恢复原值/无草稿状态。另在隔离项目浏览器改文字、保存、重开和撤销通过，无pageerror。
- 60项测试与TypeScript通过。证据脚本、截图、视频报告：`projects/local-workbench/qa/object-editing/`。本轮首轮隔离测试public使用软链接导致cp错误，已改为复制资源后重新验证；不是用户项目数据错误。
- 用户项目创作revision仍为19，仅选中状态按页面操作更新。支持已声明并映射的对象；不是任意组件自动可编辑，也未实现鼠标拖拽、完整代码回滚或防止有磁盘权限的代码故意绕过参数。


# 启动与恢复补齐 · 2026-09-17

用户批准的本轮范围为一键启动、重开恢复、包装渲染取消/重试。

- TypeScript 与 62 项单元/集成检查通过，新增任务所有权测试覆盖取消后清理前不可插入新任务、旧取消不能作用于新任务、停止服务后不再接收新任务。
- 隔离项目真实 Remotion 渲染期间取消成功，再次生成得到 341 帧、11.366667 秒的有声 MP4；FFmpeg 全片解码无错误。
- 故意破坏隔离项目的 TSX 后编译失败，上一成功视频仍保留；恢复代码后模拟磁盘残留 rendering 状态，重建服务后持久化为失败并显示中断原因。
- 浏览器实际检查包装页面恢复段落、人物对象、已保存的 0.8 大小和 2.5 秒位置；刷新后仍一致。点击取消渲染显示已取消及重新生成提示。
- 同一浏览器重开恢复粗剪工作区及 3.2 秒播放位置。上述浏览器渲染/修改操作使用隔离制作项目，未改用户制作方案。
- 实测证据：`projects/local-workbench/qa/startup-recovery/result.json`、`media.json`、`ui.json`、`ui.png` 与隔离项目。测试不是用户审美认可。

边界：编译步骤的底层 API 不支持中途取消，因此显示「正在停止」并等待该步完成；渲染和 FFmpeg 处理响应取消。取消入口覆盖包装样片/全片，不覆盖粗剪生成与开发工具里的动画实验。保存状态恢复不等于未保存草稿自动恢复；完整工程迁移、源码回滚与全面性能验收仍待后续。


启动器交付验收：

- 6 项独立启动器测试通过：命令解析、复用、陌生服务拒绝、独立 tsx 启动参数、带令牌停止、活跃启动锁保留。共计本轮 62 + 6 = 68 项检查通过；启动脚本语法通过。
- 实机启动 PID 99258 后启动命令退出，服务继续运行；再次 start 和 status 返回同一个 PID。通过停止脚本正常停止，再从 `/private/tmp` 执行绝对路径打开脚本，服务重启为 PID 99302。
- 无令牌 POST /api/shutdown 返回 403；不会误停服务。
- 重开后当前用户工程仍为 revision19，intro/person/7.5667秒位置保留。制作方案、粗剪工程与原样片 project.json 的 SHA-256 与启动前一致。
- 实机证据：`projects/local-workbench/qa/startup-recovery/launcher-live.json`。服务用本机端口独立运行，不是登录自启动守护服务；源码或依赖被移动仍需修正路径或依赖。


# DJI原片实际制作

用户106.346667秒4K原片通过工作台API导入。复用同哈希素材350词时间标记，校正明显错字并用本机Whisper复核末段；5段保留范围生成85.4333秒粗剪。粗剪revision17，制作方案revision20。全片7段设计，其中转写与同步粗剪已制作15.3秒/459帧1080p可编辑样片。719个音频包与同区间独立处理对照逐包一致，已检查实际关键帧、4处剪点和浏览器属性变化/放弃还原。62项检查与TypeScript通过。其余6段动画待样片反馈后制作，未代替用户审美与听感认可。

实片修复：未实现的自定义场景不再送入旧三段播放器；上一成功视频校验源素材，异素材旧片隐藏，同素材旧片仍保留，文件不删除。证据与交付见 projects/local-workbench/productions/dji-20260820-20260917/README.md。示意轨道不冒充软件录屏或真实波形，工作代理源画面不冒充4K母版。

## 2026-09-17 · 逐段确认后合成

- 实现：`segment-reviews.ts` 独立渲染/确认历史；`segment-assembly.ts` 校验并复用确认画面、统一完整音轨；`production-composition.ts` 将画面指纹与 HTTP/审阅界面改动分离；Production UI 和 CLI 接入。
- 28 项相关测试通过；最终 TypeScript 检查通过。
- 最终实际回归：`projects/local-workbench/verification/segment-flow-IE3KIa/verification.json`。2 个独立段落、132 帧、208 音频包；拼接画面逐帧等同原段落，音轨包与完整测试粗剪一致；中途参数修改阻止过期确认/合成，另一段认可保留，重启后确认恢复。
- 已通过 Computer Use 在主工作台实际触发第五段渲染，看到制作中→待确认；播放新版后确认按钮可用，切到历史版本确认禁用；第六段定位到 36.23 秒，回第五段仍可独立播放。没有点击正式项目确认按钮，测试确认仅在隔离工程。
- 当前服务 PID 5175，127.0.0.1:4340 已就绪，浏览器保留第五段样片页面。原粗剪、生产设计、认可 v2 视频 SHA-256 前后完全一致，正式 `segment-reviews.json` approvals 仍为 0。既有整片认可不自动伪造为新流程里的逐段操作记录。
- 用户对新工作流的实际使用验收仍待后续；原认可视频和旧渲染保留。跨段衔接仍需最终整片观看，任意跨段转场不会自动生成。
