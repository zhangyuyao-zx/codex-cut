# 全部待办收尾验收

> 2026-09-17 用户决定：本轮停止继续测试与扩展开发，保留当前版本供实际使用。下方未完成项转为后续使用中按实际问题优化，不作为当前交付的阻塞项，也不改写为已通过。后续归入组件收尾、使用验收、交付收尾三个范围；未经新指示不恢复批量测试。

用户目标：完成此前整轮升级的剩余事项。保留现有轻量工作台、Codex对话、六步创作、逐段审阅与合成；不改为内置Agent，不恢复797项库，不做透明层导出。

下列项目只有实现、适当自动检查及对应实际操作/媒体证据齐备才完成。测试通过不是用户审美认可。

单段缺素材隔离已验证：一个段落缺文件不阻止其他段编辑、保存重开、实际生成和审阅确认；全片仍阻止合成。见 [单段素材隔离](SCENE_MATERIAL_ISOLATION.md)。该增量不减少下列完整清单；长期播放与原生视频AX控制崩溃的环境复核仍属稳定性验收。

| 项目 | 需要交付与验收 | 当前状态 |
|---|---|---|
| 194组件 | 每项实际预览、添加、调参、保存重开、连续动画及导出；错误与素材需求明确 | 三帧全库出图、194项5秒/150帧/960×540连续输出及全解码通过；单位图复核后修正格子与发光并单独复测。完整逐项操作与全规格实际验收待完成 |
| 收藏 | 收藏/取消、过滤、重启持久化 | 已实现；隔离浏览器收藏/过滤/刷新/取消通过，磁盘持久化检查通过 |
| 动画复用 | 源码依赖、叠加组件、外部媒体完整封装；新口播重绑定；用户编辑保留 | 静态媒体、叠加组件及已提供素材已保存，重新绑定/预览/应用/撤销/导出通过；独立.cutanimation包HTTP/CLI与实际UI/36帧原声输出通过；新捕获素材逐槽替换、目标文字/锁保留、原子应用/撤销及重新保存通过实际UI与36帧原声输出；旧固定快照明确区分。任意动态资产仍待补齐 |
| 连续表达与动作 | 动作与口播绑定、起中终态、相邻场景衔接、实际带音频审阅 | 基础规则与审阅已接入，系统验收待完成 |
| 多素材剪辑 | 导入多素材、顺序与区间编辑、各素材转写/词ID、撤销、预览与导出一致 | 已部署4340；隔离UI保存/刷新、复制/重排选中、撤销、空态恢复、冲突/删除草稿保护及生成按钮通过。真实口播素材40秒/1200帧解码、边界画面及三段音频信号通过；全素材转写交互和持续使用仍待验收 |
| 变换面板 | 位置/大小/裁剪/旋转/音量，即时预览、持久化、撤销及导出一致 | 已部署4340；位置/缩放/裁剪/旋转/音量接入。实际变换导出、crop→contain、UI保存/刷新及百分比输入通过，控件与旋转分离；极端参数的逐项UI/导出对比仍待完成 |
| 局部重渲染 | 源码/参数/素材/剪辑变化的实际影响范围，未受影响视频复用，失败与取消保护 | 细粒度scene key、稳定内部无损源、外部内容和实际组件依赖摘要已接入；145项检查和类型检查通过。真实72帧验证未改段像素一致并复用原确认画面字节合成；文案/词时间/相同生成/取消重试通过。外部素材及源码审计退化/恢复、环境缺失/恢复矩阵通过，粗剪编码为mock；旧range仍保守，不迁移确认。持续使用验收待完成 |
| 完整场景转场 | 相邻场景实际画面参与，帧数和原声时间保持正确，边界验帧 | 实现与隔离实际UI/输出通过并部署4340：三段各36帧/108帧整片与拼接、AAC时间/数据包一致，完整构图淡入/推移、尾部静默、两帧短段、缺文件/依赖/用户编辑保护和取消重试通过。未认可技术样片；见SCENE_TRANSITIONS.md |
| 历史与交付 | 源码与工程匹配快照、校验、恢复到新目录、运行依赖与路径迁移 | 自动快照、校验及新目录恢复通过4094文件实测；产品内备份入口、草稿/任务/写入保护与流式捕获接入；隔离1499文件实际UI恢复并重新生成36帧1080p原声视频通过。运行路径可配置；跨机器可迁移交付仍待验证 |
| 稳定性 | 持续播放、切换/调参、反复渲染取消/重试、内存/响应、正常重开 | 待系统验证 |
| 总体流程 | 新隔离项目走完导入→粗剪→设计→逐段制作→审阅→合成，保留用户认可边界 | 待最终综合验收 |

旧App项目采用逐项兼容检查和副本迁移，不能静默覆盖或删除。跨机器验证须明确实际测试环境；同机新目录验证不可冒充另一台物理机器。当前正式影片与工程不作为破坏性测试对象。
